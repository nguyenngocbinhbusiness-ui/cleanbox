name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: |
          pyinstaller --onefile --windowed --name CleanBox --icon src/assets/icon.ico --add-data "src/assets;assets" --path src src/main.py

      - name: Create portable ZIP
        run: |
          $version = "${{ github.ref_name }}"
          Compress-Archive -Path dist/CleanBox.exe -DestinationPath "CleanBox-$version-windows-x64-portable.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/CleanBox.exe
            CleanBox-${{ github.ref_name }}-windows-x64-portable.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release Summary
        run: |
          echo "## 🎉 Release ${{ github.ref_name }} Complete!" >> $env:GITHUB_STEP_SUMMARY
          echo "" >> $env:GITHUB_STEP_SUMMARY
          echo "### Artifacts:" >> $env:GITHUB_STEP_SUMMARY
          echo "- ``CleanBox.exe`` - Windows executable" >> $env:GITHUB_STEP_SUMMARY
          echo "- ``CleanBox-${{ github.ref_name }}-windows-x64-portable.zip`` - Portable package" >> $env:GITHUB_STEP_SUMMARY
          echo "" >> $env:GITHUB_STEP_SUMMARY
          echo "> **Note:** CleanBox is a Windows-only utility (uses Windows-specific APIs)." >> $env:GITHUB_STEP_SUMMARY
